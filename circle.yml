machine:
  node:
    version: 7.10.0

general:
  branches:
    ignore:
      - gh-pages

deployment:
  production:
    branch: master
    commands:
      - "npm run build"
      - "~/$CIRCLE_PROJECT_REPONAME/script/deploy"
